import React from 'react';
import ReactDOM from 'react-dom';

const TextField = () => <p>Test React App</p>;

ReactDOM.render (<TextField />, document.getElementById ('root'));

console.log ('hi', process.env.NODE_ENV);
